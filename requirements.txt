# Python deps for the feet-intent lane controller (pose_input.py + input_server.py).
# The Node websocket transport in server/ is separate (see package.json).
opencv-python>=4.8
numpy>=1.24
websockets>=12.0
mediapipe>=0.10.9   # default pose backend; uses mp.solutions if available,
                    # otherwise Tasks API (auto-downloads model on first run)
